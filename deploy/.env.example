# Compose interpolation only — hostnames, no secrets. Copy to `.env`.
# Point all three at the instance's public IP with A records first (README §3).
USER_HOST=prism.example.com
ADMIN_HOST=admin.prism.example.com
API_HOST=api.prism.example.com
